<div data-role="page">

	<div data-role="header" data-position="inline">
    <h1>Session Attended</h1>
    <a href="<%= Rho::Application.startURI %>" class="ui-btn-left" data-icon="home" data-direction="reverse" <%= "data-ajax='false'" if is_bb6 %>>
      Home
    </a>
    <a href="<%= url_for :action => :new %>" class="ui-btn-right" data-icon="plus">
      New
    </a>
	</div>

  <div data-role="content">
    <ul data-role="listview">
      <% @session_attendeds.each do |session_attended| %>
      
          <li>
           Session ID <a href="<%= url_for :action => :show, :id => session_attended.object %>">
              <%= session_attended.SessionID %>
            </a>
          </li>
      
      <% end %>
    </ul>
  </div>

</div>
